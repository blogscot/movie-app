{% extends 'templates/default.twig' %}

{% block title %}Login{% endblock %}

{% block content %}
  <div class="row">
    <div class="col-sm-offset-3 col-sm-9">
      <h2>Please login</h2>
    </div>
  </div>
   <form class="form-horizontal" action="{{ urlFor('auth.login.post') }}" method="post" autocomplete="off">
     <div class="form-group">
        <label class="control-label col-sm-offset-3 col-sm-1" for="identifier">Username </label>
        <div class="col-sm-6">
          <input class="form-control" type="text" name="identifier" id="identifier">
          <div class="user-warning">
            {% if errors.has('identifier') %} {{ errors.first('identifier') }} {% endif %}
          </div>
        </div>
     </div>
     <div class="form-group">
        <label class="control-label col-sm-offset-3 col-sm-1" for="password">Password: </label>
        <div class="col-sm-6">
          <input class="form-control" type="password" name="password" id="password">
          <div class="user-warning">
            {% if errors.has('password') %} {{ errors.first('password') }} {% endif %}
          </div>
        </div>
     </div>
     <div class="form-group">
       <div class="col-sm-offset-3 col-sm-1">
         <button class="btn btn-primary" type="submit">Login</button>
       </div>
     </div>
     <div class="form-group">
       <div class="col-sm-offset-3 col-sm-3">
         <a href="#">Forgotten your password?</a>
       </div>
     </div>
      <input type="hidden" name="{{ csrf_key }}" value="{{ csrf_token }}">
   </form>
{% endblock %}
